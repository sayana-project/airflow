# Dépendances pour le projet Airflow ML (Python 3.13 compatible)
flask>=3.0.0
pandas>=2.0.0
numpy>=2.0.0
scikit-learn>=1.5.0
scipy>=1.13.0
python-dotenv>=1.0.0
pendulum>=3.1.0
requests>=2.32.0
# Airflow dependencies (to be installed via Docker)
# apache-airflow==3.0.5 (full installation via Docker Compose)
# psycopg2-binary>=2.9.0
# redis>=5.0.0
# celery>=5.3.0
# flower>=2.0.0